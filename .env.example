# Mẫu cấu hình môi trường — sao chép thành .env rồi điền key thật
#   PowerShell:  Copy-Item .env.example .env
# .env (file thật) đã được .gitignore bỏ qua; .env.example thì commit được vì không chứa key.
OPENAI_API_KEY=sk-...
